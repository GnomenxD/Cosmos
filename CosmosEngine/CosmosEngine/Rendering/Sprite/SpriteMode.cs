
namespace CosmosEngine
{
	public enum SpriteMode
	{
		/// <summary>
		/// This texture only contains a single sprite.
		/// </summary>
		Single,
		/// <summary>
		/// This is a sprite sheet and can contain multiple sprites, all sprites on the same sprite sheet should be sliced the same size.
		/// </summary>
		Multiple,
	}
}
